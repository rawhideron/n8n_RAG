# ============================================================
# n8n RAG Platform — Kubernetes Secrets Template
# ============================================================
# Copy this file to k8s/.env.secrets and fill in real values.
# k8s/.env.secrets is git-ignored — never commit real secrets.
#
# Generate strong keys with:
#   openssl rand -hex 32          (for 64-char hex keys)
#   openssl rand -base64 32       (for cookie secret)
# ============================================================

# --- PostgreSQL ---
POSTGRES_USER=root
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_DB=n8n

# --- n8n ---
# Must be a stable secret — changing it invalidates stored credentials

N8N_ENCRYPTION_KEY=4k4n4jn4js
N8N_USER_MANAGEMENT_JWT_SECRET=5lmk2dls

# --- oauth2-proxy ---
# MUST match the client secret configured in the n8n-rag Keycloak realm.
# After running k8s/keycloak/realm-import-job.yaml, update the client
# secret in the Keycloak admin UI at:
#   https://goodmanreunion.duckdns.org/keycloak/admin
#   Realm: n8n-rag → Clients → n8n-oauth2-proxy → Credentials
OAUTH2_PROXY_CLIENT_SECRET=CHANGE_ME_OAUTH2_PROXY_SECRET

# Cookie secret — must be exactly 16, 24, or 32 bytes (base64-encoded)
# Generate with: openssl rand -base64 32 | tr -d '\n'
OAUTH2_PROXY_COOKIE_SECRET=CHANGE_ME_RUN_openssl_rand_base64_32
